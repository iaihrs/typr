/*
function handleSave(event) {
    event.preventDefault();

    const data = new FormData(event.target);
    const wordNumValue = data.get('number of words')
    const wordPopValue = data.get('word popularity')
    console.log({ wordNumValue, wordPopValue });
}

const select = document.querySelector('form');
select.addEventListener('submit', handleSave);
*/